{
  "name": "react-mono-demo",
  "version": "1.2.1",
  "main": "dist/bundle.js",
  "files": [
    "dist"
  ],
  "license": "MIT",
  "scripts": {
    "build": "rimraf dist && webpack --config ../../webpack.config.js",
    "deploy": "yarn build && yarn publish"
  },
  "publishConfig": {
    "registry": "http://localhost:4873/"
  },
  "dependencies": {
    "prop-types": "^15.7.2",
    "react": "^17.0.1",
    "react-dom": "^17.0.1"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "webpack": "^5.24.2",
    "webpack-cli": "^4.5.0"
  }
}
