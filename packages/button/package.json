{
  "name": "react-mono-button",
  "version": "1.6.0",
  "main": "dist/bundle.js",
  "files": [
    "dist"
  ],
  "license": "MIT",
  "scripts": {
    "build": "rimraf dist && webpack --config ../../webpack.config.js",
    "deploy": "yarn build && yarn publish --new-version minor"
  },
  "publishConfig": {
    "registry": "http://localhost:4873/"
  },
  "dependencies": {
    "prop-types": "^15.7.2",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "styled-components": "^5.2.1"
  },
  "resolutions": {
    "styled-components": "^5"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "webpack": "^5.24.2",
    "webpack-cli": "^4.5.0"
  }
}
